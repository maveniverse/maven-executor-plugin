Maven Executor Plugin
=====================

A **Maven 3 and 4 plugin** to execute Maven builds. The plugin allows you to execute Maven builds from within a 
Maven build, which can be useful for various use cases, such as integration testing.
